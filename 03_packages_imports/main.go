package main

// import brings external pkgs into the file that u r working or where u actually need it
import (
	"fmt"
	"math"
)

func main(){
	// pkgName.FunctionName -> call a function from a pkg

	fmt.Println("square root of 25: ",math.Sqrt(25));
}